{
  "amino": {
    "pubsub": {
      "enable": true,
      "driver": "redis",
      "options": {
        "host": "localhost",
        "port": 6379
      }
    },
    "queue": {
      "enable": true,
      "driver": "amqp",
      "options": {
        "url": "amqp://localhost"
      }
    },
    "request": {
      "enable": true,
      "driver": "http"
    }
  }
}